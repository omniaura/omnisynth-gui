import omnisynth
import os

OMNISYNTH_PATH = os.getcwd().replace(
    'omnisynth-gui', 'omnisynth-dsp/').replace("\\", "/")
SC_PROCESS_NAME = "sclang.exe"
SC_SYNTH_PROCESS_NAME = "scsynth.exe"
